Bonjour monsieur,

Je vais ici faire la description de mon projet.

Le dossier database_files contient les fichiers de ma base de donnée (logement.db et logement.sql) ainsi qu'un fichier texte expliquant les commandes curl utilisées.

Le dossier html_css_files contient les fihiers html de chacune de mes 5 pages (j'ai eu un problème que j'explique dans le fichier Python a éxécuté avec le .css donc il n'y en a pas).

Le dossier Images contient une image que mon code n'arrive pas à trouver, si vous comprenez pourquoi n'hésitez pas à me le dire.

Le dossier py_files contient les fichiers Python de chaque partie du projet. Celui a éxécuté afin que le serveur fonctionne est le fichier "Partie3.py".


Pour le serveur, la page d'accueil est sensé contenir une image, mais comme expliqué auparavant, mon code ne la trouve pas je ne sais pas pourquoi.

La page "Consommation de votre logement" est la page qui m'a pris le plus de temps, notamment afin que les graphiques soient propres, lisibles, et disposent de plusieurs échelles de temps.
Cette page fonctionne en intégralité.

La page "État des capteurs" affiche la liste des capteurs dans la base de données actuelle. Elle fonctionne également correctement. Il y a possibilité de rajouter d'autres éléments également.

La page "Économies réalisées" n'est qu'une page Front End, il n'y a aucun back-end et donc aucun contact avec la base de données.

De même la page "Configuration" n'est qu'une page Front End.